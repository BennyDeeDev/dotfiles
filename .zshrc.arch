DOTFILES="$HOME/Repos/dotfiles"
[[ -f "$DOTFILES/.zshrc.common" ]] && source "$DOTFILES/.zshrc.common"

alias yay="paru"
alias xc="wl-copy"

pacman-bundle() {
  "$DOTFILES/scripts/pacman-bundle.sh"
}

reboot-arch() {
  "$DOTFILES/scripts/reboot-to-arch.sh"
}
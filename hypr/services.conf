# Services managed by the OS on NixOS (systemd user units)
# On Arch these need to be started manually via exec-once
exec-once = uwsm-app -- hypridle
exec-once = uwsm-app -- mako
exec-once = uwsm-app -- waybar
exec-once = uwsm-app -- swayosd-server

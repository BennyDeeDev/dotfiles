$terminal = ghostty
$fileManager = nautilus
$browser = brave --password-store=basic --restore-last-session
$editor = code
$passwordManager = keepassxc
$music = spotify
$menu = hyprlauncher
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

bind = CTRL, Q, killactive

bind = CTRL SHIFT, T, exec, $terminal
bind = CTRL SHIFT, F, exec, $fileManager
bind = CTRL SHIFT, B, exec, $browser
bind = CTRL SHIFT, E, exec, $editor
bind = CTRL SHIFT, P, exec, $passwordManager
bind = CTRL SHIFT, S, exec, $music
bind = CTRL, SPACE, exec, $menu
bind = CTRL SHIFT, Q, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = CTRL SHIFT, V, togglefloating
bind = CTRL SHIFT, J, togglesplit
bind = CTRL SHIFT, P, pseudo
bind = CTRL SHIFT, W, exec, pkill waybar || waybar &

# Move focus with mainMod + arrow keys
bind = SUPER, left, movefocus, l
bind = SUPER, right, movefocus, r
bind = SUPER, up, movefocus, u
bind = SUPER, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9
bind = SUPER, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = CTRL SHIFT, 1, movetoworkspace, 1
bind = CTRL SHIFT, 2, movetoworkspace, 2
bind = CTRL SHIFT, 3, movetoworkspace, 3
bind = CTRL SHIFT, 4, movetoworkspace, 4
bind = CTRL SHIFT, 5, movetoworkspace, 5
bind = CTRL SHIFT, 6, movetoworkspace, 6
bind = CTRL SHIFT, 7, movetoworkspace, 7
bind = CTRL SHIFT, 8, movetoworkspace, 8
bind = CTRL SHIFT, 9, movetoworkspace, 9
bind = CTRL SHIFT, 0, movetoworkspace, 10

# Resize windows with keyboard
bind = SUPER, code:20, resizeactive, -100 0    # - key (expand left)
bind = SUPER, code:21, resizeactive, 100 0     # = key (shrink left)
bind = SUPER SHIFT, code:20, resizeactive, 0 -100  # shrink up
bind = SUPER SHIFT, code:21, resizeactive, 0 100   # expand down

# Multimedia keys for volume and LCD brightness (with SwayOSD)
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

bindeld = ,XF86AudioRaiseVolume, Volume up, exec, $osdclient --output-volume raise
bindeld = ,XF86AudioLowerVolume, Volume down, exec, $osdclient --output-volume lower
bindeld = ,XF86AudioMute, Mute, exec, $osdclient --output-volume mute-toggle
bindeld = ,XF86AudioMicMute, Mute microphone, exec, $osdclient --input-volume mute-toggle
bindeld = ,XF86MonBrightnessUp, Brightness up, exec, asdbctl up -s 5 && brightness=$(asdbctl get | awk '{print $2}') && progress=$(awk "BEGIN{printf \"%.2f\", $brightness/100}") && $osdclient --custom-icon display-brightness-symbolic --custom-progress "$progress" --custom-progress-text "${brightness}%"
bindeld = ,XF86MonBrightnessDown, Brightness down, exec, asdbctl down -s 5 && brightness=$(asdbctl get | awk '{print $2}') && progress=$(awk "BEGIN{printf \"%.2f\", $brightness/100}") && $osdclient --custom-icon display-brightness-symbolic --custom-progress "$progress" --custom-progress-text "${brightness}%"

# Precise 1% multimedia adjustments with Alt modifier
bindeld = ALT, XF86AudioRaiseVolume, Volume up precise, exec, $osdclient --output-volume +1
bindeld = ALT, XF86AudioLowerVolume, Volume down precise, exec, $osdclient --output-volume -1
bindeld = ALT, XF86MonBrightnessUp, Brightness up precise, exec, asdbctl up -s 1 && brightness=$(asdbctl get | awk '{print $2}') && progress=$(awk "BEGIN{printf \"%.2f\", $brightness/100}") && $osdclient --custom-icon display-brightness-symbolic --custom-progress "$progress" --custom-progress-text "${brightness}%"
bindeld = ALT, XF86MonBrightnessDown, Brightness down precise, exec, asdbctl down -s 1 && brightness=$(asdbctl get | awk '{print $2}') && progress=$(awk "BEGIN{printf \"%.2f\", $brightness/100}") && $osdclient --custom-icon display-brightness-symbolic --custom-progress "$progress" --custom-progress-text "${brightness}%"

bindld = , XF86AudioNext, Next track, exec, $osdclient --playerctl next
bindld = , XF86AudioPause, Pause, exec, $osdclient --playerctl play-pause
bindld = , XF86AudioPlay, Play, exec, $osdclient --playerctl play-pause
bindld = , XF86AudioPrev, Previous track, exec, $osdclient --playerctl previous